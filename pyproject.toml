[project]
name = "cluster-kg"
version = "0.1.0"
description = "Knowledge graph tooling for galaxy clusters"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "rdflib>=7.0.0",
]

[tool.uv]
package = false
